interface:
  display_name: "Code Reviewer"
  short_description: "Independent review before risky completion steps"
  default_prompt: "Use $code-reviewer to review my recent code changes and return a PASS, FLAG, or BLOCK verdict."

policy:
  allow_implicit_invocation: true
